/**
 * @jest-environment node
 */
describe('STAC API Extensions', () => {
  describe('applyStacFieldsExtension', () => {
    it('WIP: it should apply the STAC fields extension to the provided CMR STAC response', async () => {
      expect(true).toBeTruthy();
    });
  });
});
